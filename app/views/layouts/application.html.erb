<!DOCTYPE html>
<html lang="en">
  <%= render 'layouts/dochead' %>

  <body>
    <nav class="navbar navbar-toggleable-md navbar-light bg-faded">
      <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>">Brain</a>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
          <li class="nav-item active">
            <a class="nav-link" href="<%= atoms_path %>">Atoms <span class="sr-only">(current)</span></a>
          </li>
        </ul>
        <%= form_for Atom.new, html: { class: "form-inline my-2 my-lg-0" } do |f| %>
          <%= f.text_field :title, placeholder: "Name of Atom", class: "form-control mr-sm-2" %>
          <%= f.submit "Add", class: "btn btn-outline-success my-2 my-sm-0" %>
        <% end %>
      </div>
    </nav>

    <%= yield %>

    <%= javascript_include_tag "application" %>
  </body>
</html>
